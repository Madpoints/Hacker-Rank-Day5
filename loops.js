function main() {
    var n = parseInt(readLine());
    var result;
    var i = 1
    
    while (i <= 10) {
      result = i * n;
      console.log(n + " x " + i + " = " + result);
      i++; 
    };

}